# Rust (fgraph/)
/fgraph/target/
**/*.pdb

# Python (fgraph-gui/)
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
*.egg

# fgraph-gui dev-only mock harness scratch (nothing to ignore here yet, kept
# vendored assets under web/vendor/ tracked intentionally -- offline-first)

# PyInstaller build output (built via fgraph-gui/build_exe.ps1, not committed)
fgraph-gui/build/
fgraph-gui/dist/

# Inno Setup installer output (built via fgraph-gui/build_installer.ps1, attach
# to GitHub Releases instead of committing -- it's a repackaging of fgraph-gui.exe)
fgraph-gui/installer_output/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Logs / local scratch
*.log
